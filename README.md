# weather-pipeline
Weather monitoring pipeline built in Python using the ETL (Extract, Transform, Load) pattern. Fetches live weather data from an API, transforms and structures the data, and generates alerts when temperature thresholds are exceeded.
